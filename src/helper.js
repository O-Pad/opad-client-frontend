function createFile(){
    // inform file tracker
    // file tracker creates a file and returns a unique id
    // add file to list of opened files in UI
}

function openFile(fileId) {
    // while(File not received)
    //      fetch user ip, port  from file tracker
    //      contact user id and get file
    //      if user not available
    //          inform tracker of offline user
    //          get new user from tracker
}

function closeFile(fileId) {
    // inform file tracker that we are going offline on that file
    // remove file from list of 
}

function rabbitMQWillCallThis(message) {
    // 
    //

}